# Copy this file to .env only if you need to override local defaults.
# The database data is stored in the Docker volume named siih_postgres_data.

POSTGRES_DB=siih
POSTGRES_USER=siih_app
POSTGRES_PASSWORD=siih_dev_password
POSTGRES_PORT=5432

BACKEND_PORT=8080
SPRING_PROFILES_ACTIVE=local
